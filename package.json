{
  "name": "inicie-realtime-platform",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@10.6.2",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "format": "turbo run format",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down -v",
    "load:baseline": "artillery run load-tests/artillery/baseline.http.yml && artillery run load-tests/artillery/baseline.mqtt.yml",
    "load:stress": "artillery run load-tests/artillery/stress.http.yml && artillery run load-tests/artillery/stress.mqtt.yml"
  },
  "devDependencies": {
    "artillery": "^2.0.30",
    "artillery-engine-mqtt": "^1.2.3",
    "turbo": "^2.0.14",
    "typescript": "^5.6.3"
  }
}
